services:
  app:
    build: .
    container_name: ai-goofish-monitor-app
    init: true
    ports:
      - "8010:8010"
    env_file:
      - .env
    volumes:
      - .:/app
      - /app/dist
    restart: unless-stopped
